package Housing;

abstract class AbsSpecs{
	/** @param p - the person inputted
	 *  @ param people - the list of people to be checked
	 *  @return - true if caroline and michael are in list, false if they are not
	 */
	def carMichael(people : List[Person]) : Boolean;

}